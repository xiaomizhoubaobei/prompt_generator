interface:
  display_name: "Prompt Generator"
  short_description: "Repo-specific patterns and workflows for prompt_generator"
  default_prompt: "Use the prompt_generator repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true
